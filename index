<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A.G. Campbell - Cyber Grid Reality</title>
    <style>
        /* Global Styles */
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            background: black;
            overflow: hidden;
            position: relative;
        }

        /* Background: Black & White Cyber Grid */
        .grid {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: repeating-linear-gradient(0deg, rgba(255,255,255,0.1) 0px, rgba(255,255,255,0.1) 1px, transparent 2px, transparent 50px), 
                        repeating-linear-gradient(90deg, rgba(255,255,255,0.1) 0px, rgba(255,255,255,0.1) 1px, transparent 2px, transparent 50px);
            z-index: 1;
            animation: gridMove 5s linear infinite;
        }

        @keyframes gridMove {
            0% { transform: scale(1) translateY(0px); }
            50% { transform: scale(1.05) translateY(5px); }
            100% { transform: scale(1) translateY(0px); }
        }

        /* Glitch Effect for Blue Text */
        @keyframes glitch {
            0% { text-shadow: 2px 2px #00FFFF, -2px -2px #0033FF; }
            25% { text-shadow: -3px -3px #00FFFF, 3px 3px #0033FF; }
            50% { text-shadow: 3px -3px #00FFFF, -3px 3px #0033FF; }
            75% { text-shadow: -3px 3px #00FFFF, 3px -3px #0033FF; }
            100% { text-shadow: 2px 2px #00FFFF, -2px -2px #0033FF; }
        }

        h1 {
            font-size: 2.8em;
            font-family: 'Courier New', Courier, monospace;
            color: #00FFFF;
            animation: glitch 1.5s infinite alternate;
            position: relative;
            z-index: 2;
            letter-spacing: 2px;
            text-transform: uppercase;
        }

        /* Text Box – High-Tech Terminal Look */
        .container {
            max-width: 750px;
            padding: 30px;
            border: 3px solid white;
            border-radius: 10px;
            background: rgba(0, 0, 0, 0.85);
            position: relative;
            z-index: 2;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.6), 0 0 50px rgba(255, 255, 255, 0.3);
            animation: pulseGlow 3s infinite alternate ease-in-out;
        }

        @keyframes pulseGlow {
            0% { box-shadow: 0 0 10px white, 0 0 25px rgba(255, 255, 255, 0.4); }
            100% { box-shadow: 0 0 30px white, 0 0 50px rgba(255, 255, 255, 0.7); }
        }

        p {
            font-size: 1.2em;
            color: white;
            opacity: 0.85;
        }

        /* Glowing Button */
        a {
            font-size: 1.1em;
            text-decoration: none;
            color: black;
            padding: 12px 24px;
            border: 2px solid #00FFFF;
            border-radius: 5px;
            background: #00FFFF;
            transition: all 0.3s ease-in-out;
            position: relative;
            z-index: 3;
            box-shadow: 0 0 10px #00FFFF;
        }

        a:hover {
            background-color: black;
            color: #00FFFF;
            transform: scale(1.1);
            box-shadow: 0 0 20px #00FFFF;
        }

    </style>
</head>
<body>

    <!-- Grid Background -->
    <div class="grid"></div>

    <!-- Main Content -->
    <div class="container">
        <h1>Welcome to the World of A.G. Campbell</h1>
        <p>Author, Creator, Computer Engineer, and Explorer of the Mind</p>
        <p>Discover stories, thoughts, and creations that challenge the limits of reality.</p>
        <a href="#about" title="Learn More">Learn More About Me</a>
    </div>

</body>
</html>
